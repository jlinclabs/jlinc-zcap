'use strict';

//const jwt = require('@jlinc/jwt');
const {
  //generateVerifiedRequest,
  generateActor,
} = require('../helpers');
//const zcap = require('../../');

describe('createDelegable', function() {
  context('when given a invoker DID and capability keys', function(){
    it('should return a delegable ZCAP', function(){
      const  {
        //did: invokerDid,
        //signingKeys: capabilityKeys,
      } = generateActor();
    });
  });


});
